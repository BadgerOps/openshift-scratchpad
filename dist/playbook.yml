---
- hosts: all
  become: yes
  tasks:
    - name: Create the foo file with rw-r--r-- permissions
      ansible.builtin.file:
        path: /etc/foo.conf
        owner: foo
        group: foo
        mode: '0644'
